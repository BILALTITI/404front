"use client";

/**
 * Three.js orb was removed. This stub keeps any `import { HeroOrb } from "./HeroOrb"`
 * from breaking without adding the `three` dependency.
 */
interface HeroOrbProps {
  scrollVelocityRef?: React.MutableRefObject<number>;
}

export function HeroOrb(_props: HeroOrbProps) {
  return null;
}
